void main (){
  //syntax 
  // condition ? if true : if not true

  //-------------------------------
  // instead of using if else we can use ternary operator
  int age = 20;
  var valid = (age >= 18) ? 'voter' : 'Not Voter' ;
  print("You are $valid");

  int days = 20;
   (days >= 18) ? print("You are above 18") :  print("You are under 18") ;



}